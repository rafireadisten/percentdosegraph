kind = "web"
previewPath = "/"
title = "PercentDoseGraph"
version = "1.0.0"
id = "artifacts/dosegraph"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/dosegraph run dev"

[services.env]
PORT = "3000"
BASE_PATH = "/"
